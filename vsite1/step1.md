Generate a web site in TypeScript with Express. 

 - It should have a simple API that allows CRUD operations on a set of customers. Make a Customer interface that can be used in the API calls, and implement it with a simple array of Customer objects. Initialize the array with 5 mock customers.

 - It should have a web interface that allows listing the customers

 - Include an appropriate .gitignore file

 - Add the ability to start the solution by running an npm script

 - We are using the REST Client extension for Visual Studio Code. Add an http folder that contains a file called test.http that exercises the REST API.
